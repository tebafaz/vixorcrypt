name: CI

on: push
jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v3
    - name: Lint
      run: |
        npm install eslint@latest
        npx eslint "src/**" \
          --config .eslintrc.json